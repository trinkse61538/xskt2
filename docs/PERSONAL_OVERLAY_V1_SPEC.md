# PERSONAL OVERLAY V1.0 SPEC

Personal is not an independent prediction engine.

```text
Universal Ranking → Personal Overlay → Personal Ranking
```

V1 final score:

```text
80% Universal + 20% Personal elemental overlay
```

Only the Universal Top 20 can be reranked into Personal Top 5. Profile name/label is never used as numerology.

The Bát Tự overlay derives four pillars using the same deterministic calendar conventions as Universal, forms a five-element distribution, then creates a balance affinity. This is explicitly a project-level elemental-balance overlay; it should not be represented as a complete traditional Dụng Thần implementation.
